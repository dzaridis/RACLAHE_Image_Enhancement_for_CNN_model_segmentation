# EUCAIM Docker Compose Environment Configuration
# Copy this file to .env and adjust paths as needed

# Dataset path (input data) - will be mounted as read-only
DATASET_PATH=./input

# Persistent home path (output and user data)
PERSISTENT_HOME=./output

# Shared folder path (for collaboration)
SHARED_FOLDER=./shared

# Optional: Override default paths inside container
# INPUT_DIR=/home/ds/datasets
# OUTPUT_DIR=/home/ds/persistent-home/output
# WEIGHTS_PATH=/home/ds/BBOX_WEIGHTS/WHOLE GLAND/checkpoint_external.h5

# Optional: Resource limits
# CPUS_LIMIT=4
# MEMORY_LIMIT=8G
# CPUS_RESERVATION=2
# MEMORY_RESERVATION=4G

